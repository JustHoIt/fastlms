spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:43306/minicampus
    driver-class-name: org.mariadb.jdbc.Driver
    username: m_user
    password: a1234

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: zalop3128@cku.ac.kr
    password: yyzbhihdjeldskwu
    properties:
      mail:
        smtp:
          starttls:
            enable: true

logging:
  level:
    root: debug

mybatis:
  mapper-locations: mybatis/**/*.xml
  configuration:
    map-underscore-to-camel-case: true #    언더바 표기법을 카멜 표기법으로 바꿔줌
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 로그찍기



